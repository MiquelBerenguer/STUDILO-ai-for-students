# --- INFRAESTRUCTURA CORE ---
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.1
# CRÍTICO: Necesitamos v2.9+ para los validadores estrictos del Worker
pydantic==2.9.0
pydantic-settings==2.1.0

# --- BASE DE DATOS (PostgreSQL) ---
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0

# --- COLAS Y MENSAJERÍA (RabbitMQ) ---
pika==1.3.2
# CRÍTICO: Versión robusta para reconexiones automáticas
aio-pika==9.4.0
tenacity==8.2.3

# --- ALMACENAMIENTO (MinIO) ---
minio==7.2.0

# --- CACHÉ (Redis) ---
redis==5.0.1

# --- PROCESAMIENTO PDF E IMÁGENES ---
python-multipart==0.0.9
pypdf==4.0.1
pdf2image==1.16.3
pillow==10.1.0
aiofiles==23.2.1
Jinja2==3.1.3 

# --- IA Y VECTORES ---
# Mantengo Gemini por si lo usas en otros lados
google-generativeai>=0.5.0
# CRÍTICO: Subido a 1.9.0 para tener .search() asíncrono
qdrant-client>=1.9.0
# CRÍTICO: Necesario para Structured Outputs (.parse)
openai>=1.50.0
tiktoken==0.6.0

# --- UTILIDADES CIENTÍFICAS Y OTROS ---
numpy==1.26.4
pandas==2.2.0
scipy==1.12.0
requests==2.31.0
httpx==0.27.0
structlog==24.1.0
httpcore==1.0.5

# --- GENERACIÓN PDF (WeasyPrint) ---
weasyprint==61.2
pydyf==0.10.0
cffi==1.16.0