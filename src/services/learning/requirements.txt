# --- CORE WEB ---
# Fijamos versiones para evitar sorpresas
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.9.0
pydantic-settings==2.1.0
requests==2.31.0
python-dotenv==1.0.1

# --- BASE DE DATOS & VECTORES ---
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
asyncpg==0.29.0
# CRÍTICO: Soluciona el error 'AttributeError: search'
qdrant-client>=1.9.0

# --- MENSAJERÍA (RabbitMQ) ---
aio-pika==9.4.0
pika==1.3.2

# --- ALMACENAMIENTO ---
minio==7.2.0

# --- INTELIGENCIA ARTIFICIAL ---
# CRÍTICO: Soluciona el error 'Invalid Schema' y .parse()
openai>=1.50.0
tenacity==8.2.3
tiktoken==0.6.0

# --- GENERACIÓN DE PDF Y TEMPLATES ---
jinja2==3.1.3
# Mantenemos reportlab por si tienes lógica legacy, pero usamos WeasyPrint
reportlab>=4.0.0
weasyprint==61.2

# --- DEPENDENCIAS DE BAJO NIVEL ---
pydyf==0.10.0
cffi==1.16.0
# Necesario para el manejo de imágenes en PDFs
pillow==10.1.0